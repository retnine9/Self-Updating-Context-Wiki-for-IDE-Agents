---
session: a1b2c3d4
date: 2026-06-10
title: Debug 401 errors on the tasks API
---

## Decisions
- Standardize on `Authorization: Bearer` header per OpenAPI spec instead of `X-Api-Token`.
- Add regression tests in `tests/test_auth.py` for valid and missing headers.

## Errors
- GET /api/tasks returned 401 for all requests because middleware read wrong header name.

## Rejected approaches
- None documented.

## Constraints identified
- API auth must follow OpenAPI Bearer scheme.

## Technical debt noted
- None.

## Open questions
- None.

## Summary
Fixed 401 errors by aligning JWT middleware with the Bearer header and added regression tests.
